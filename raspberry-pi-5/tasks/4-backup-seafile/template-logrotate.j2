{{ backup_log_file_path }} {
    size 1M               # triggers when file is bigger than 1M
    rotate 10             # keep 10 files
    missingok             # does not throw error if the file is missing
    notifempty            # does not rotate if the file is empty
}
